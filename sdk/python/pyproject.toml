[build-system]
requires = []
build-backend = "_build_backend"
backend-path = ["."]

[project]
name = "workworld-sdk"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["websockets>=14,<18"]
